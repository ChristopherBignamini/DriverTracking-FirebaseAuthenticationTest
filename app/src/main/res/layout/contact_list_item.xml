<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="wrap_content"
              android:weightSum="1">

    <LinearLayout
        android:id="@+id/contact_name_phone_view"
        android:orientation="vertical"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="0.9">

        <TextView
            android:id="@+id/contact_name_text_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="4dp"/>

        <TextView
            android:layout_below="@id/contact_name_text_view"
            android:id="@+id/contact_phone_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="4dp"/>

    </LinearLayout>

    <!-- TODO: rebuild all the layout -->
    <ImageView
        android:layout_toRightOf="@id/contact_name_phone_view"
        android:id="@+id/contact_status_image"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:src="@drawable/user_status_offline"
        android:layout_weight="0.1"/>

</LinearLayout>